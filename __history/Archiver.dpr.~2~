program Archiver;

uses
  Vcl.Forms,
  MainUnit in 'MainUnit.pas' {MainForm},
  ArchiveDialog in 'ArchiveDialog.pas' {ArchiveForm};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TMainForm, MainForm);
  Application.Run;
end.
